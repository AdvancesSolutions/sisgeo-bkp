# API usada pelo frontend. Em dev, se não definir, o app usa /api (proxy do Vite).
VITE_API_URL=http://localhost:3000

# Opcional: para ver dados de produção localmente, crie .env.local com:
# PROXY_API_TARGET=https://api.sigeo.advances.com.br
# (não defina VITE_API_URL no .env.local para que o proxy seja usado)
