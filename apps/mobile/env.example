# URL da API SIGEO (mesma do painel web em produção).
# Padrão: CloudFront da AWS (dados e dashboard em produção).
# Para API local: use o IP da sua máquina na rede (ex: http://192.168.1.10:3000)
EXPO_PUBLIC_API_URL=https://dapotha14ic3h.cloudfront.net
