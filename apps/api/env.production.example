# Copie para .env.production e preencha com dados do RDS
# Depois: pnpm --filter @sigeo/api run db:migrate:employee-id
# (com dotenv carregando .env.production)

DB_HOST=seu-rds-endpoint.region.rds.amazonaws.com
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=sua_senha_rds
DB_NAME=sigeo
